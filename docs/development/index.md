# Development

```{toctree}
:maxdepth: 1

architecture
testing
docs
contributing
releasing
roadmap
citing
```

{doc}`architecture` maps the modules and the data that flows between them, which is
the first thing to read before changing the code. {doc}`testing` describes the test
suite and the continuous-integration workflow, {doc}`docs` how to build this
documentation and regenerate its figures, {doc}`contributing` how to submit changes,
{doc}`releasing` how versions are tagged and published, {doc}`roadmap` what is planned
and what lives on branches, and {doc}`citing` how to cite the code.
